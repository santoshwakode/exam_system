<%= form_for(@question_detail) do |f| %>
  <% if @question_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_detail.errors.count, "error") %> prohibited this question_detail from being saved:</h2>

      <ul>
      <% @question_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exam_id %><br>
    <%= f.number_field :exam_id %>
  </div>
  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_area :question %>
  </div>
  <div class="field">
    <%= f.label :question_type %><br>
    <%= f.text_field :question_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
